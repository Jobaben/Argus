{
  "name": "argus",
  "version": "0.4.0",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jobaben/Argus.git"
  },
  "bugs": {
    "url": "https://github.com/Jobaben/Argus/issues"
  },
  "homepage": "https://github.com/Jobaben/Argus#readme",
  "private": true,
  "description": "Argus \u2014 the all-seeing monitor for your Claude Code agents, jobs, history and results.",
  "type": "module",
  "bin": {
    "argus": "bin/argus.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "contracts",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm -w server run dev",
    "dev:web": "npm -w web run dev",
    "build": "npm -w web run build && npm -w server run build",
    "start": "npm -w server run start",
    "test": "npm -w server run test && npm -w web run test",
    "typecheck": "npm -w contracts run typecheck && npm -w server run typecheck && npm -w web run typecheck",
    "lint": "npm -w contracts run lint && npm -w server run lint && npm -w web run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run typecheck && npm run lint && npm run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.2"
  }
}
